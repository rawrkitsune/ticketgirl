// Load main client (do not remove this.)
const { client } = require('./src/client/');

// Load main commands functions and file paths (do not remove this.)
const { handleCommands } = require('./src/handlers/handleCommands.js')